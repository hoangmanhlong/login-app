<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="sliderItem"
            type="com.example.loginapp.adapter.overview_slider_adapter.SliderItem" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_horizontal|center_vertical"
        android:orientation="vertical">

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/guide"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            app:layout_constraintGuide_percent="0.8" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toTopOf="@id/guide"
            android:orientation="vertical"
            android:gravity="center"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                imgRes="@{sliderItem.image}"
                tools:src="@drawable/option1"
                android:layout_width="@dimen/_180sdp"
                android:layout_height="@dimen/_180sdp"
                android:contentDescription="@null" />

            <TextView
                tools:text="Track your Orders"
                style="@style/title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center_horizontal"
                android:layout_marginTop="@dimen/_25sdp"
                android:paddingBottom="5dp"
                android:textSize="25sp"
                android:fontFamily="@font/inter_bold"
                android:text='@{sliderItem.title}' />

            <TextView
                style="@style/content"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center_horizontal"
                android:paddingHorizontal="@dimen/_8sdp"
                android:fontFamily="@font/inter_regular"
                android:textSize="16sp"
                tools:text="Free and fast delivery for all meals above ₹100."
                android:layout_marginTop="@dimen/_8sdp"
                android:text='@{sliderItem.description}' />

        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
